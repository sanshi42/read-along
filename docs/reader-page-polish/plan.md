# 阅读页最终打磨 Plan

## 实现方案

- 以 `.impeccable/critique/2026-06-16T12-12-05Z__web-src-routes-readerpage-tsx.md` 为优先级输入，先处理 P1，再处理与同一文件高度相关的 P2。
- 将播放器与顶部导航的实际尺寸纳入可读区域计算，保证当前句滚动后落在固定 chrome 之间。
- 保留句子点击和键盘选择能力，但让正文默认保持 prose 语义，只通过当前聚焦句和附加标签表达可操作性。
- sticky nav 在标题滚出后显示压缩材料标题和句子进度，避免用户恢复到中段时失去上下文。
- 使用现有 `DESIGN.md` 和 `web/src/styles.css` token；不引入新视觉系统，不新增装饰性模式。

## 关键决策

- 核心阅读路径按接近 flagship 的质量线处理；快捷键和进度概览等增强能力留作后续 Topic。
- 本次只修改阅读页相关代码、样式和测试，避免触碰播放模式业务规则。

## Topic 级验证

- 运行前端相关单元测试。
- 运行 `make check`。
- 运行 impeccable detector 覆盖本次前端目标。
- 如能启动本地服务，使用浏览器检查桌面与移动端阅读页主要交互。
