# 采用 SQLModel 时无损接管现有本地数据库

Read Along 已经存在真实本地数据和多种历史 SQLite schema。采用 SQLModel 和 Alembic 时，必须通过桥接迁移无损保留现有材料、来源身份、结构化正文、阅读进度和导入任务；不允许通过清空并重建数据库完成切换。这个选择增加首次迁移与测试成本，但避免破坏本地材料库，并让 Alembic 在接管后成为唯一的 schema 演进机制。
